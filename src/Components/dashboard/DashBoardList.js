import React from 'react';

const DashBoardList = ({
    cities,
    onCitySelect
}) => {
    const renderedList = cities.map((city)) => {
        return ( < FlightShow / > );
    };
}
export default DashBoardList;